import React from 'react';

const FillComponentDetails = (props) => {
    // const {setPopUp} = props;
    return (
        <div>
            <input type="text" placeholder="Component Name"></input>
        </div>
    )
}

export default FillComponentDetails
